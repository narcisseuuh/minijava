// NE PAS MODIFIER car c'est un fichier de référence.
/** Stress Test lexicale et syntaxique (avec tableau). syntaxiquement correct */
class Test309 {
  public static void main(String[] args) { {} }
}

/** comment with 2* **/  /****/
class Bar {
  public int Compute() {
    a = (138 + jiosfd * meep - bar < a)[33];
    b = new int[10];
    return b.length[20].length.length+b.length ;
  }
}

class Foo extends Meep {
  public int[] yarrr() {
    Meep a;
    a = new Meep();
    a[2] = (aa.length + bb.length * xx.getSize()).yarrr();
    return false;
  }
}

class FooBar {
    public int params(int i, int[] nums, Foo foo) {
       Bar bar;
       int local;
       local = nums.length;
       if (i < local)
          local = nums[i];
       else
          local = 0;
       return local;
    }
}


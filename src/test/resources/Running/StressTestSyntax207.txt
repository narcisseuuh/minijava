// NE PAS MODIFIER car c'est un fichier de référence.
/** Stress Test lexicale et syntaxique (sans tableau). syntaxiquement correct */
class Test303 {

  // This should be ignored
  /* So should this /*

  and this still */

  public static void main(String[] args) {
    System.out.println(new Bar(). /* comment */ //
                       Compute(false*true+x_y_123+this-!true<!!!!!true*((((1))))));
  }
}
class Empty {}
class Empty2 extends Empty {}
class Point {  int x;  int y; }
class FlipFlop {  boolean value; }
class LinkedList { int value; LinkedList next; }

/** comment with 2* **/  /**/ /***/ /****/
class Bar {
  int _α_;
  int abc123;
  public int /*start name*/Compute/*end name*/ ( ) {
    int
      cc
      ;
    cc=new Bar().a().b(1).c();
    a=xxx.doit(1, 2, 3, 4, this.doit()) + xxx.doit(1) - ((!!true));
    if (__meep00) {
      a = (138&&v + (a * jiosfd) * (meep - bar) < a && !a < !!b);
    } else {
      while (((c)))
        a = 5;
    }
    if (mm) {
      /* empty block */ } else
      b = a; /* mono instruction block */

    System.out.println(5);
    System.out /* comment */ .println(38);
    System
    .out // comment
    .println(42);

    int a;
    a = foo[foo[3*2+foo[4]]*2].length<foo[true];
    return b * this.length + !!!!!true && (((5)));
  }
  boolean b;
  public int getX() { return x; }
  int c;
  public int get2X() { return 2*x; }
  boolean b;
}

// A single line comment before EOF
